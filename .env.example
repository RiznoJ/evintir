# WATCHSTANDER — example environment file.
# v2 uses no API keys (RSS feeds are free and public), so this file exists
# only to establish the habit early: real secrets go in .env (gitignored),
# placeholders go here. If a keyed API is added later, document it like:
# EXAMPLE_API_KEY=your-key-here
